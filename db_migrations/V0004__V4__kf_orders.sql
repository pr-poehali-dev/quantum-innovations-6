CREATE TABLE kf_orders (id SERIAL PRIMARY KEY, buyer_id INTEGER NOT NULL REFERENCES kf_users(id), post_id INTEGER REFERENCES kf_posts(id), title VARCHAR(256) NOT NULL, description TEXT, budget NUMERIC(12,2), status VARCHAR(32) NOT NULL DEFAULT 'new', created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());