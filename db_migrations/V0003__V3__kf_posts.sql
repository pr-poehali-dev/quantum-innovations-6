CREATE TABLE kf_posts (id SERIAL PRIMARY KEY, author_id INTEGER NOT NULL REFERENCES kf_users(id), title VARCHAR(256) NOT NULL, description TEXT, image_url TEXT, price NUMERIC(12,2), is_for_sale BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW());