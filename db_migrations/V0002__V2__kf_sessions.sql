CREATE TABLE kf_sessions (id SERIAL PRIMARY KEY, user_id INTEGER NOT NULL REFERENCES kf_users(id), token VARCHAR(128) NOT NULL UNIQUE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), expires_at TIMESTAMPTZ NOT NULL DEFAULT (NOW() + INTERVAL '30 days'));