CREATE TABLE kf_messages (id SERIAL PRIMARY KEY, author_id INTEGER NOT NULL REFERENCES kf_users(id), channel VARCHAR(64) NOT NULL DEFAULT 'general', content TEXT NOT NULL, is_hidden BOOLEAN NOT NULL DEFAULT FALSE, created_at TIMESTAMPTZ NOT NULL DEFAULT NOW());